# 服务器端口
server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  port: 8080

# 数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TakeOutxx?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  ai:
    openai:
      api-key: sk-8e7a7ded76ee46b58a8e9822638f14f6
      base-url: https://api.deepseek.com/v4
      chat:
        model: deepseek-chat

# JWT配置
jwt:
  secret: NothingAccomplished13260696371snyhdaijunjie
  expiration: 3600000
  token-prefix: Bearer
  header: Authorization

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

role:
  redirect:
    0: /consumer/home
    1: /merchant/home
    2: /rider/home
